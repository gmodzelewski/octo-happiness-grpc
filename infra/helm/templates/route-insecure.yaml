apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    {{- include "app.defaultLabels" . | indent 4 }}
  name: {{ .Release.Name }}-route-insecure
spec:
  tls:
    termination: edge
  port:
    targetPort: main
  to:
    kind: Service
    name: {{ .Release.Name }}-service