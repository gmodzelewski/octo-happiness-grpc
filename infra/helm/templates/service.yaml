apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "app.defaultLabels" . | indent 4 }}
  name: {{ .Release.Name }}-service
spec:
  ports:
  - name: main
    port: 8080
    targetPort: main
  - name: grpc
    port: 8443
    targetPort: grpc
  selector:
    {{- include "app.matchLabels" . | indent 4 }}